# Store source modules

This folder is the editable source of the generated store. Update `identity.ts`, `catalog.ts`, `operations.ts`, `template.ts` and `admin.ts` directly; no runtime configuration file is required.

Each product in `catalog.ts` has a `cover` image and an ordered `images` gallery. Keep the cover aligned with the first image in the gallery.

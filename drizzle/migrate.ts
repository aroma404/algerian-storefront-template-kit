import mysql from "mysql2/promise";
const migration = "CREATE TABLE IF NOT EXISTS products (id VARCHAR(64) PRIMARY KEY, slug VARCHAR(191) NOT NULL UNIQUE, name VARCHAR(191) NOT NULL, category VARCHAR(120) NOT NULL, price_dzd INT NOT NULL, compare_at_dzd INT NULL, description TEXT NOT NULL, image_url TEXT NOT NULL, images_json JSON NOT NULL, options_json JSON NOT NULL, tags_json JSON NOT NULL, stock_quantity INT NOT NULL DEFAULT 0, is_active TINYINT(1) NOT NULL DEFAULT 1, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP, INDEX products_category_active (category, is_active)); CREATE TABLE IF NOT EXISTS inventory_movements (id BIGINT AUTO_INCREMENT PRIMARY KEY, product_id VARCHAR(64) NOT NULL, quantity_delta INT NOT NULL, reason VARCHAR(80) NOT NULL, reference VARCHAR(64) NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, INDEX inventory_product_created (product_id, created_at)); CREATE TABLE IF NOT EXISTS orders (id BIGINT AUTO_INCREMENT PRIMARY KEY, order_reference VARCHAR(64) NOT NULL UNIQUE, customer_name VARCHAR(191) NOT NULL, phone VARCHAR(64) NOT NULL, address TEXT NOT NULL, delivery_id VARCHAR(80) NOT NULL, delivery_label VARCHAR(191) NOT NULL, delivery_fee_dzd INT NOT NULL, payment_method VARCHAR(120) NOT NULL, merchandise_dzd INT NOT NULL, total_dzd INT NOT NULL, status VARCHAR(32) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, INDEX orders_phone_created (phone, created_at), INDEX orders_status_created (status, created_at)); CREATE TABLE IF NOT EXISTS order_items (id BIGINT AUTO_INCREMENT PRIMARY KEY, order_id BIGINT NOT NULL, product_id VARCHAR(64) NOT NULL, product_name VARCHAR(191) NOT NULL, selected_option VARCHAR(120) NOT NULL, unit_price_dzd INT NOT NULL, quantity INT NOT NULL, line_total_dzd INT NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, INDEX order_items_order (order_id));";
if (!process.env.DATABASE_URL) throw new Error("DATABASE_URL is required for db:migrate");
const connection = await mysql.createConnection(process.env.DATABASE_URL); await connection.query(migration); await connection.end(); console.log("Store migration completed");
